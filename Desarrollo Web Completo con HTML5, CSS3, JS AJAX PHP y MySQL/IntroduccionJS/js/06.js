// OBJETO MATH

let resultado;

resultado = Math.PI; // numero pi
resultado = Math.round(2.7); // redondea
resultado = Math.ceil(2.9); // redondea hacia arriba siempre
resultado = Math.floor(2.9) // redondea hacia abajo siempre 
resultado = Math.sqrt(144); // Raiz cuadrada
resultado = Math.abs(-144); // convierte en positivo
resultado = Math.min(3, 5, 1 ,8 , 10); // saca el minimo de un listado de nuneros
resultado = Math.max(3, 5, 1 ,8 , 10); // saca el maximo de un listado de nuneros
resultado = Math.floor(Math.random() * 30); // saca el maximo de un listado de nuneros

console.log(resultado);